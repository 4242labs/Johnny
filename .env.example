# johnny machine-local secrets. Copy to `.env` (gitignored) and fill in.
#   cp .env.example .env
# config.sh auto-sources `.env`, so both the CLI and the Stop hook see these.

# ElevenLabs (engine: eleven). Get a key: https://elevenlabs.io/app/settings/api-keys
ELEVEN_API_KEY=

# Optional overrides:
# VOICE_ENGINE=kokoro
# ELEVEN_VOICE_ID=21m00Tcm4TlvDq8ikWAM
